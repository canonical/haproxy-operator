on:
  pull_request:

jobs:
  rtd-docs-checks:
    uses: canonical/operator-workflows/.github/workflows/docs_rtd.yaml@ci/update-docs-rtd-workflow
    secrets: inherit
    with:
      enable-sphinx-python-dependency-build-checks: false
      python-version: '3.12'

