groups:
  - name: haproxy_node_lost
    rules:
      - alert: HaproxyNodeLostError
        expr: count_over_time({%%juju_topology%%} |~ `Node (.*) is no longer a peer` [5m]) > 0
        for: 0s
        labels:
            severity: error
        annotations:
            summary: A node was lost
