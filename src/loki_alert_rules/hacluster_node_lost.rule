groups:
  - name: haproxy_node_lost
    rules:
      - alert: HaproxyNodeLostError
        expr: rate({%%juju_topology%%} |~ ``Node (.*) is no longer a peer` [5m]) > 0
        for: 0s
        labels:
            severity: page
        annotations:
            summary: A node was lost