# --- Release notes artifact ----

schema_version: 1
change:
  - title: Updated library to add more attributes
    author: Thanhphan1147
    type: minor
    description: The `subdomain` attribute has been replaced with the `hostname` and `additional_hostnames` attribute. The charm now sends an empty list as the `proxied_endpoints` when the requirer data is ignored.
    urls: 
      pr: https://github.com/canonical/haproxy-operator/pull/152
      related_doc: 
      related_issue: 
    internal: false
    highlight: false
