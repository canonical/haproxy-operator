# --- Release notes artifact ----

schema_version: 1
changes:
  - title: Disabled sending host headers in `httpchk` so the charm doesn't refuse to proxy incoming requests
    author: Thanhphan1147
    type: bugfix
    description: Disabled sending host headers in `httpchk` so the charm doesn't refuse to proxy incoming requests.
    urls: 
      pr: https://github.com/canonical/haproxy-operator/pull/111
      related_doc: 
      related_issue: 
    visibility: public
    highlight: false
